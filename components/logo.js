import React from 'react'
import PropTypes from 'prop-types'

Logo.defaultProps = {}

Logo.propTypes = {}

export default function Logo({}) {
  return <img src="/images/logo.png" className="logo" />
}
